import React from "react";
import ReactDOM from "react-dom/client";
import RakuenInterface from "@rakuen/interface";

ReactDOM.createRoot(document.getElementById("app") as HTMLElement).render(
	<React.StrictMode>
		<RakuenInterface />
	</React.StrictMode>,
);
